/* Logical Operators */

const logicalOperators = {
    and: "AND (&&) - Both must be true to give true.",
    or: "OR (||) - Only one needs to be true to give true.",
    not: "NOT (!) - Flips the value. True becomes false, false becomes true."
  };
  
  console.log(logicalOperators);
